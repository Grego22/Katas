# <!-- Given a string, write a function that returns the string with a question mark ("?")
# appends to the end, unless the original string ends with a
#  question mark, in which case, returns the original string. -->

# My Solution

def ensure_question(s)
    s[-1] == "?" ? s : s+"?"
end


# Cool Solutions
def ensure_question(s)
  s.end_with?("?") ? s : s+"?"
end

def ensure_question(s)
  s.chomp('?') + '?'
end

def ensure_question(s)
  # Code here
   s = s + "?" unless s.end_with?("?")
 s
end

def ensure_question(s)
  s.match(/.*\?\z/) ? s : s << "?"
end
